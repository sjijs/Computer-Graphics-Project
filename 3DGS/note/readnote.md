# 3DGS论文即相关技术学习笔记

## AI分析概括原文论文

### **1. 研究背景与问题**
辐射场（Radiance Field）方法通过多视角图像重建3D场景并合成新视角，已成为视觉合成领域的重要技术。但现有方法存在明显缺陷：
- **高质量方法（如Mip-NeRF360）**：需训练48小时，渲染速度仅0.071 fps（远非实时）；
- **快速方法（如InstantNGP、Plenoxels）**：训练时间短（5-26分钟），但渲染质量低于前者，且1080p分辨率下无法达到实时（≥30 fps）。

**核心目标**：实现**高质量（媲美Mip-NeRF360）** + **实时渲染（1080p下≥30 fps）** + **训练时间与快速方法相当**。


### **2. 核心创新：3D高斯溅射（3D Gaussian Splatting）**
作者提出三个关键技术，结合了“连续体积表示”的优化优势和“显式点表示”的渲染效率：

#### **（1）3D高斯场景表示**
- **定义**：用3D高斯分布（而非离散点或体素）表示场景，每个高斯由**均值（3D位置）**、**各向异性协方差矩阵（描述形状）**、**不透明度α**、**球谐系数（SH，描述视角相关颜色）** 定义。
- **优势**：
  - 继承连续体积表示的可微性，便于优化；
  - 避免空区域的无效计算（比NeRF的体素采样更高效）；
  - 可投影为2D椭圆“溅射斑（splat）”，通过α混合快速渲染（类似点渲染，但更灵活）。


#### **（2）自适应优化与密度控制**
- **参数优化**：通过梯度下降优化高斯的位置、协方差、α、SH系数，目标是最小化渲染图像与真实图像的差异（损失函数为L1+D-SSIM）。
- **密度控制（动态增删高斯）**：
  - **克隆（Clone）**：对小高斯（欠重建区域），沿位置梯度方向复制，补充细节；
  - **分裂（Split）**：对大高斯（过重建区域），分裂为两个小高斯，提升精度；
  - **裁剪**：移除不透明度α过低的高斯，保持紧凑性。
- **结果**：最终场景由100万-500万个高斯表示，兼顾精度与效率。


#### **（3）快速可见性感知渲染器**
- **核心思路**：基于瓦片（16×16像素）的光栅化，结合GPU快速排序实现高效α混合。
- **步骤**：
  1. 将3D高斯投影到2D图像空间，得到椭圆溅射斑；
  2. 按“瓦片ID+深度”对溅射斑排序（确保可见性顺序）；
  3. 逐瓦片渲染：每个瓦片内按深度顺序混合溅射斑的颜色与不透明度，达到目标不透明度（α≈1）时停止，减少计算。
- **优势**：支持各向异性溅射斑，渲染速度达135 fps（1080p），且反向传播高效（无梯度计算数量限制）。


### **3. 实验结果：性能碾压现有方法**
#### **（1）核心指标对比**
| 方法         | 训练时间 | 渲染速度（fps） | PSNR（质量，越高越好） |
|--------------|----------|-----------------|------------------------|
| Mip-NeRF360  | 48小时   | 0.071           | 24.3                   |
| InstantNGP   | 7分钟    | 9.2             | 22.1                   |
| Plenoxels    | 26分钟   | 8.2             | 21.9                   |
| 本文方法（Ours） | 51分钟 | 135             | 25.2（超Mip-NeRF360）  |

- **质量**：训练51分钟时，PSNR（25.2）略高于Mip-NeRF360（24.3），视觉细节更清晰（如植被、墙面纹理）；
- **速度**：1080p下渲染速度达93-135 fps，远超现有方法；
- **效率**：训练时间与InstantNGP相当（5-51分钟），远低于Mip-NeRF360。


#### **（2）消融实验：各组件的必要性**
- **各向异性协方差**：移除后（仅用各向同性高斯），PSNR下降约0.8 dB，无法准确表示细长结构（如树枝）；
- **密度控制（克隆+分裂）**：禁用后，PSNR下降约2 dB，欠/过重建区域无法修正；
- **无SH系数**：无法建模视角相关颜色（如高光），PSNR下降约0.7 dB。


### **4. 局限性与未来方向**
- **局限性**：
  - 观测不足区域易产生 artifacts（如背景模糊）；
  - 偶尔出现“闪烁”（大高斯突然被裁剪）；
  - 内存消耗较高（大型场景峰值达20 GB）。
- **未来工作**：抗锯齿、正则化优化、压缩高斯模型以减少内存。


### **总结**
这篇文献的核心贡献是**用3D高斯作为辐射场的“桥梁”**：既保留了连续表示的优化灵活性，又通过溅射斑渲染实现了实时速度。其方法在质量、速度、效率上的平衡，为实时3D场景重建与新视角合成提供了里程碑式的解决方案，后续在AR/VR、数字孪生等领域有重要应用潜力。

## Splatting
- **体渲染方法**
  - ![alt text](image.png)
  - ![alt text](image-1.png)
- **选择“雪球”**
  - 点云中的点没有体积
  - 所以需要一个核，以其为中心进行“膨胀”
  - 高斯核有很好的数学性质：
    - ![alt text](image-2.png)
  - **3D高斯为椭球**
    - ![alt text](image-3.png)
    - ![alt text](image-4.png)
    - 当e的指数为定值（高斯函数取到某个取值时）高斯函数在三维空间中表现出一个椭球壳
    - 又因为高斯函数取值为0~1，所以高斯函数整体在三维空间中表现为实心的椭球
- **各向同性&各向异性**
  - ![alt text](image-5.png)
- **任意高斯可以看作是标准高斯通过仿射变换得到**
  - ![alt text](image-6.png)
  - 3D高斯中正是协方差矩阵控制了高斯的形状
- **协方差矩阵因此可以使用旋转与缩放来表达**
  - ![alt text](image-7.png)
  - A为仿射变换矩阵，可以拆为旋转与缩放矩阵

### 从3D到像素 - 光栅化渲染管线
- ![alt text](image-8.png)
- ![alt text](image-9.png)

## 3D高斯的观测变换
- **3D高斯的核心即为其均值与协方差矩阵**
- ![alt text](image-10.png)
- **重点**
  - 对于协方差矩阵不可进行透视矩阵变换
  - 因为透视矩阵为非线性变换
  - 如果使用非线性变化会使得协方差矩阵形变

### 雅可比矩阵
- ![alt text](image-11.png)
- 对非线性变换的局部线性近似
- 确定了一个点，即可使用*导数*在其附近足够小的位置进行线性逼近
- ![alt text](image-12.png)
- 对协方差矩阵进行的投影变换近似
- ![alt text](image-13.png)
- 雅可比矩阵应用过程：
  - ![alt text](image-14.png)

### 3D高斯球颜色
- ![alt text](image-15.png)
- **球谐函数**
  - ![alt text](image-16.png)
  - 颜色与观测角度建立联系
  - 球谐函数是一组 球面上的正交基函数（类似傅里叶基）
    - *类似于高斯求积公式中使用到的由施密特正交化得到的正交函数组*
  - **即球面坐标系下，以(θ，φ)自变量的任一球面函数，都可使用一组球谐函数来近似**
  - 球谐函数的阶数（通常记为$𝐿$或最大阶数$𝑙$）是按照近似精度的需求来决定
  - 思想类似于高斯求积公式，用一组已知的函数/采样点，去近似表示一个复杂对象
    - 在球谐函数在全局光照的应用中，每个像素就类似于采样点，最终近似出一个可以表示全局光的函数
    - ![alt text](image-17.png)
- **3D高斯球的球谐函数中相关系数为3维，因为最后输出期望为一颜色值，所以系数需三维**
- **3D高斯球使用球谐函数的作用是在物理空间中不同方向上表示不同的颜色，而全局光照的球谐函数作用是颜色空间中表示各个方向不同的颜色**

## 球谐函数近似全局光照过程
### 1. 问题背景

* 环境光照（environment map 或者 HDR 天空盒）给出的是**从每个方向来的光线强度** $L(\omega)$，其中方向 $\omega$ 在球面上。
* 在渲染中，我们常常需要计算某个点的**入射辐照度**（irradiance）：

$$
E(n) = \int_{\Omega} L(\omega) \, \max(0, n \cdot \omega) \, d\omega
$$

其中 $n$ 是法线方向。

* 直接用环境贴图做积分很昂贵，因为每个像素都要对整个贴图做积分。

所以，人们希望：
**把环境光贴图投影到一组球谐基函数上，得到一个低维近似表示**，这样渲染时只需几个系数运算，就能高效近似积分。

---

## 2. 球谐展开的核心思想

球谐函数 $\{Y_{lm}(\omega)\}$ 是一组球面上的正交基函数。
任何球面函数（比如光照分布 $L(\omega)$）都可以展开为：

$$
L(\omega) \approx \sum_{l=0}^{L} \sum_{m=-l}^{l} c_{lm} Y_{lm}(\omega)
$$

其中 $c_{lm}$ 是系数，取决于环境贴图。

---

## 3. 如何把环境贴图转为球谐系数

球谐系数定义为：

$$
c_{lm} = \int_{\Omega} L(\omega) Y_{lm}(\omega) \, d\omega
$$

在实际中，这个积分通过**对环境贴图采样并加权求和**近似。

* 假设环境贴图在长方形图像上，像素索引为 $(i,j)$，对应方向为 $\omega_{ij}$，像素颜色为 $L_{ij}$，像素覆盖的立体角为 $\Delta \omega_{ij}$。
* 那么离散近似就是：

$$
c_{lm} \approx \sum_{i,j} L_{ij} \, Y_{lm}(\omega_{ij}) \, \Delta \omega_{ij}
$$

注意这里的 $\Delta \omega_{ij}$ 取决于投影方式（比如等距柱状投影时是 $\sin\theta \Delta\theta \Delta\phi$）。

---

## 4. 渲染时如何利用球谐系数

一旦我们有了球谐系数 $c_{lm}$，计算**某个方向的辐照度**时，不用对环境贴图做积分，而是直接：

$$
L(\omega) \approx \sum_{l=0}^{L} \sum_{m=-l}^{l} c_{lm} Y_{lm}(\omega)
$$

甚至对于 **Lambertian 漫反射辐照度**，有经典结果：只需要 **到二阶球谐（L=2，共9个系数）** 就能很好近似。
这是 Ramamoorthi & Hanrahan 2001 的结果（经典论文 *An Efficient Representation for Irradiance Environment Maps*）。

---

## 5. 实际操作流程总结

你现在有一张环境光贴图，可以按以下步骤转换成球谐系数：

1. **读取环境贴图**（HDR 或 LDR）。
2. **遍历每个像素**：

   * 计算该像素对应的球面方向 $\omega_{ij}$（通过投影公式，比如等距柱状）。
   * 计算该像素的立体角权重 $\Delta \omega_{ij}$。
   * 对每个球谐基函数 $Y_{lm}(\omega_{ij})$，累加：

     $$
     c_{lm} += L_{ij} \, Y_{lm}(\omega_{ij}) \, \Delta \omega_{ij}
     $$
3. **存储系数**：通常每个通道（R,G,B）各有一组系数。
4. **渲染时**：用这些系数和球谐基函数直接重建光照，不再访问贴图。

---

## 6. 应用实例

* **PRT（Precomputed Radiance Transfer）**：预先计算物体对球谐基函数的响应，然后在实时渲染时和环境光的球谐系数相乘即可。
* **快速漫反射环境光**：只需要 9 个球谐系数，就能实时近似复杂 HDR 天空光照。

---

✅ 总结一句话：
在全局光照中，球谐函数的作用就是**把复杂的环境光分布投影到低维系数空间中**，这样原本高维的全局积分问题变成了**少量系数的点积运算**，极大提高了效率。

## 合成图像求得每个像素的颜色
- ![alt text](image-18.png)
- **体渲染-连续积分**
  - ![alt text](image-20.png)
  - ![alt text](image-19.png)
  - 此公式即为求某个像素的颜色即为从某个像素出发，然后求这条光线上所有粒子颜色的积分
  - 实际上计算机中采用离散计算的方式，将光线分为每一小段，认为每一小段中密度与颜色等不变
    - ![alt text](image-21.png)

## 3DGS更快核心即为更加深入应用GPU
- ![alt text](image-22.png)
- 核心渲染过程：遍历每一个像素，再通过由近到远遍历相关高斯球
- 高斯椭球对像素的影响于相机到高斯椭球的中心位置（均值点）息息相关
  - 可以理解为许多个高斯椭球弥漫在整个空间中，对于一个像素，遍历相关高斯椭球计算其对该像素的贡献度，最后得出该像素的颜色

## 机器学习部分
- ![alt text](image-23.png)
- 学习过程中主要调整的参数即为高斯椭球的各个参数
- ![alt text](image-24.png)
- ![alt text](image-25.png)
- ![alt text](image-26.png)

## 总结
- ![alt text](image-27.png)